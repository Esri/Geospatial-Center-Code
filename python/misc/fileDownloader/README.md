## FileDownloader (in `fileDownloader.py`)

Author: Jake Lucas

This python class will download a file from a website link (e.g. www.example.com/file.csv) to whatever path is specified to the class

### Requirements:
requests